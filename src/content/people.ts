import type { CommunityEvent, Partner, Speaker, Talk, TeamMember } from "./types";

/**
 * Awaiting real content from the organizers. The arrays are typed and empty
 * rather than filled with plausible-looking fakes — placeholder people have a
 * habit of surviving to production.
 */

export const speakers: Speaker[] = [];

export const talks: Talk[] = [];

export const communityEvents: CommunityEvent[] = [];

export const team: TeamMember[] = [];

export const partners: Partner[] = [];
